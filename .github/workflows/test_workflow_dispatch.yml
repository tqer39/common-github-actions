---
name: test workflow_dispatch

on:
  workflow_dispatch:
    inputs:
      env_name:
        description: '環境名を入力して "run" してください'
        default: ''
        required: true

jobs:
  github-scripts:
    runs-on: ubuntu-latest
    steps:
      - name: Comment
        run: |-
          echo "(1) inputs.env_name: ${{ inputs.env_name }}"
